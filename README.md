用k-means解决聚类问题，首先需要考虑类别数k的选择和初始种子的选择。
1、k大小的选择。可根据经验尝试不同的k,对于不同的k取值，用轮廓系数评价其效果；
2、种子点的选择。可用K-Means++的方法，以初始中心尽可能远的思想，先随机选一个点，接着选取与该点距离较远的
点作为第二个点，重复选择直至选到k个点，具体选择方法不再赘述。

由于数据集很大，单台机器无法完成工作，可以应用hadoop的MapReduce计算框架进行并行运算。
多个Mapper负责计算样本点与中心的距离，Reducer负责统计每一类的样本点并计算新的聚类中心，循环计算直至收敛。